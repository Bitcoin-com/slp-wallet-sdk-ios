language: objective-c
osx_image: xcode10.1
before_install:
  - pod repo update
install:
  - travis_wait 30 pod install
script:
  - xcodebuild -workspace SLPWallet.xcworkspace -scheme SLPWallet -destination platform\=iOS\ Simulator,OS\=12.1,name\=iPhone\ XR -enableCodeCoverage YES test
after_success:
  - bash <(curl -s https://codecov.io/bash)
